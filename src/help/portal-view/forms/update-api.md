# Update API

Use this form to update API metadata.

Updating an API changes descriptive and ownership metadata for the API record.
It does not replace the API version specification.

Important fields:

- `apiName`: user-facing API name
- `apiStatus`: lifecycle status
- `ownerPositionId`: optional position owner for team access

