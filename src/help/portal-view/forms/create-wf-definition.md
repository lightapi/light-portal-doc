# Create Workflow Definition

Use this form to create a workflow definition.

After submission, the workflow definition can be started manually or referenced
by task flows and automation.

Important fields:

- `namespace`: workflow namespace
- `name`: workflow name
- `version`: workflow version
- `definition`: workflow YAML
- `ownerPositionId`: optional position owner for team access

