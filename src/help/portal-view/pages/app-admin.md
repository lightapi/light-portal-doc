# App Admin

Use App Admin to manage client applications that own OAuth clients and instance
application links.

This page is owner-aware. Regular users should see only apps they own or can
access through their position. App administrators can see all apps for the host.

Common actions:

- create a client app
- update app metadata
- open OAuth clients for the app
- link the app to an instance

