# API Versioning

An API is the stable business record. An API version is the concrete version
that can be linked to instances, MCP tools, marketplace listings, and access
control rules.

Create the API first, then create one or more API versions under it. Operational
relationships should usually reference the API version instead of only the API.

