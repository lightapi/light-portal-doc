# Router: metricsName

The `metricsName` property specifies the name of the metric used when `metricsInjection` is enabled.

## Configuration Options

```yaml
metricsName: "router-response"
```

* **Default**: `"router-response"`.
* This allows you to customize the metric identifier that appears in your observability dashboards.
