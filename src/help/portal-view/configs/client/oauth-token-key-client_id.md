# oauth.token.key.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenKeyClientId:}`

This property configures the `oauth.token.key.client_id` setting in the `client.yml` configuration file.
