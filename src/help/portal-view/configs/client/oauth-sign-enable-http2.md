# oauth.sign.enableHttp2

Whether to use HTTP/2 multiplexing for the connection.

**Default Value (from template):** `${client.signEnableHttp2:true}`

This property configures the `oauth.sign.enableHttp2` setting in the `client.yml` configuration file.
