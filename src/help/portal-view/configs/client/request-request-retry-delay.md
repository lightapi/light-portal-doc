# request.requestRetryDelay

Time in milliseconds for token retry delays and renewals.

**Default Value (from template):** `${client.requestRetryDelay:1000}`

This property configures the `request.requestRetryDelay` setting in the `client.yml` configuration file.
