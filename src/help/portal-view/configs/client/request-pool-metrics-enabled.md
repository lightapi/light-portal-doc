# request.poolMetricsEnabled

Connection pool observability and warm-up configurations.

**Default Value (from template):** `${client.poolMetricsEnabled:false}`

This property configures the `request.poolMetricsEnabled` setting in the `client.yml` configuration file.
