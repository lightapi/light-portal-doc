# request.maxConnectionNumPerHost

Configuration property for the gateway client.

**Default Value (from template):** `${client.maxConnectionNumPerHost:1000}`

This property configures the `request.maxConnectionNumPerHost` setting in the `client.yml` configuration file.
