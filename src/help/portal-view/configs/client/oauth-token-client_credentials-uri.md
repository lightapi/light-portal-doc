# oauth.token.client_credentials.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenCcUri:/oauth2/token}`

This property configures the `oauth.token.client_credentials.uri` setting in the `client.yml` configuration file.
