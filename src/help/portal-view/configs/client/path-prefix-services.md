# pathPrefixServices

Static routing map assigning URL path prefixes to specific downstream service IDs.

**Default Value (from template):** `${client.pathPrefixServices:{}}`

This property configures the `pathPrefixServices` setting in the `client.yml` configuration file.
