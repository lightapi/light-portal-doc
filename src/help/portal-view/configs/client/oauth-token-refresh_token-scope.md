# oauth.token.refresh_token.scope

OAuth scopes requested for the token.

**Default Value (from template):** `${client.tokenRtScope:[]}`

This property configures the `oauth.token.refresh_token.scope` setting in the `client.yml` configuration file.
