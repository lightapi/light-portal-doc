# oauth.sign.server_url

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.signServerUrl:}`

This property configures the `oauth.sign.server_url` setting in the `client.yml` configuration file.
