# oauth.token.client_credentials.serviceIdAuthServers

Mapping of service IDs to specific Auth server configurations when multipleAuthServers is true.

**Default Value (from template):** `${client.tokenCcServiceIdAuthServers:{}}`

This property configures the `oauth.token.client_credentials.serviceIdAuthServers` setting in the `client.yml` configuration file.
