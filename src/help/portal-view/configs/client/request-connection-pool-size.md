# request.connectionPoolSize

Connection pooling sizes, limits, and expiration times.

**Default Value (from template):** `${client.connectionPoolSize:1000}`

This property configures the `request.connectionPoolSize` setting in the `client.yml` configuration file.
