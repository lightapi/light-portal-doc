# oauth.token.expiredRefreshRetryDelay

Time in milliseconds for token retry delays and renewals.

**Default Value (from template):** `${client.expiredRefreshRetryDelay:2000}`

This property configures the `oauth.token.expiredRefreshRetryDelay` setting in the `client.yml` configuration file.
