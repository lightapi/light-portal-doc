# oauth.token.authorization_code.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenAcUri:/oauth2/token}`

This property configures the `oauth.token.authorization_code.uri` setting in the `client.yml` configuration file.
