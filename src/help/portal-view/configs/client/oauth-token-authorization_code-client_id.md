# oauth.token.authorization_code.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenAcClientId:}`

This property configures the `oauth.token.authorization_code.client_id` setting in the `client.yml` configuration file.
