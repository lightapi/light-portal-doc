# oauth.sign.key.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.signKeyUri:/oauth2/key}`

This property configures the `oauth.sign.key.uri` setting in the `client.yml` configuration file.
