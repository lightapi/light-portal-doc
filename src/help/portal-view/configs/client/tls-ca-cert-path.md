# tls.caCertPath

Path to the certificate or key file.

**Default Value (from template):** `${client.caCertPath:}`

This property configures the `tls.caCertPath` setting in the `client.yml` configuration file.
