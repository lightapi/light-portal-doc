# oauth.token.key.enableHttp2

Whether to use HTTP/2 multiplexing for the connection.

**Default Value (from template):** `${client.tokenKeyEnableHttp2:true}`

This property configures the `oauth.token.key.enableHttp2` setting in the `client.yml` configuration file.
