# oauth.token.proxyHost

Forward proxy host or port.

**Default Value (from template):** `${client.tokenProxyHost:}`

This property configures the `oauth.token.proxyHost` setting in the `client.yml` configuration file.
