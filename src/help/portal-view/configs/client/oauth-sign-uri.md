# oauth.sign.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.signUri:/oauth2/sign}`

This property configures the `oauth.sign.uri` setting in the `client.yml` configuration file.
