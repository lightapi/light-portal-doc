# oauth.deref.server_url

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.derefServerUrl:}`

This property configures the `oauth.deref.server_url` setting in the `client.yml` configuration file.
