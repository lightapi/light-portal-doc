# oauth.token.key.audience

The target audience requested from the Auth server.

**Default Value (from template):** `${client.tokenKeyAudience:}`

This property configures the `oauth.token.key.audience` setting in the `client.yml` configuration file.
