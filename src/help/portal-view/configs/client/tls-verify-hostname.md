# tls.verifyHostname

Whether to verify the downstream server hostname against its certificate.

**Default Value (from template):** `${client.verifyHostname:true}`

This property configures the `tls.verifyHostname` setting in the `client.yml` configuration file.
