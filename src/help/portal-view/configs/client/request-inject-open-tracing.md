# request.injectOpenTracing

Configuration property for the gateway client.

**Default Value (from template):** `${client.injectOpenTracing:false}`

This property configures the `request.injectOpenTracing` setting in the `client.yml` configuration file.
