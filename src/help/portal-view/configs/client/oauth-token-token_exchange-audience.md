# oauth.token.token_exchange.audience

The target audience requested from the Auth server.

**Default Value (from template):** `${client.tokenExAudience:}`

This property configures the `oauth.token.token_exchange.audience` setting in the `client.yml` configuration file.
