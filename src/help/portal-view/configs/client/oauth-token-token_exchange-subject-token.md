# oauth.token.token_exchange.subjectToken

Configuration property for the gateway client.

**Default Value (from template):** `${client.subjectToken:}`

This property configures the `oauth.token.token_exchange.subjectToken` setting in the `client.yml` configuration file.
