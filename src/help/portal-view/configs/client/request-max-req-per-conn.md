# request.maxReqPerConn

Connection pooling sizes, limits, and expiration times.

**Default Value (from template):** `${client.maxReqPerConn:1000000}`

This property configures the `request.maxReqPerConn` setting in the `client.yml` configuration file.
