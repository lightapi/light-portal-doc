# oauth.token.key.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenKeyClientSecret:}`

This property configures the `oauth.token.key.client_secret` setting in the `client.yml` configuration file.
