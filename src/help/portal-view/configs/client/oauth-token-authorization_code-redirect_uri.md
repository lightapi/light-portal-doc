# oauth.token.authorization_code.redirect_uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenAcRedirectUri:}`

This property configures the `oauth.token.authorization_code.redirect_uri` setting in the `client.yml` configuration file.
