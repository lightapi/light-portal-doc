# request.healthCheckIntervalMs

Background health check configuration for idle connections.

**Default Value (from template):** `${client.healthCheckIntervalMs:30000}`

This property configures the `request.healthCheckIntervalMs` setting in the `client.yml` configuration file.
