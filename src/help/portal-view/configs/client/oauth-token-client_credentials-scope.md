# oauth.token.client_credentials.scope

OAuth scopes requested for the token.

**Default Value (from template):** `${client.tokenCcScope:[]}`

This property configures the `oauth.token.client_credentials.scope` setting in the `client.yml` configuration file.
