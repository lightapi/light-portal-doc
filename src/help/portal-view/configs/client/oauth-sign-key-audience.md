# oauth.sign.key.audience

The target audience requested from the Auth server.

**Default Value (from template):** `${client.signKeyAudience:}`

This property configures the `oauth.sign.key.audience` setting in the `client.yml` configuration file.
