# request.maxRequestRetry

Configuration property for the gateway client.

**Default Value (from template):** `${client.maxRequestRetry:3}`

This property configures the `request.maxRequestRetry` setting in the `client.yml` configuration file.
