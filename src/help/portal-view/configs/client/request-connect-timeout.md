# request.connectTimeout

Timeout limits in milliseconds for establishing connections and reading responses.

**Default Value (from template):** `${client.connectTimeout:2000}`

This property configures the `request.connectTimeout` setting in the `client.yml` configuration file.
