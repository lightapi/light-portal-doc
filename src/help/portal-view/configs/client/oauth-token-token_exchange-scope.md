# oauth.token.token_exchange.scope

OAuth scopes requested for the token.

**Default Value (from template):** `${client.tokenExScope:[]}`

This property configures the `oauth.token.token_exchange.scope` setting in the `client.yml` configuration file.
