# request.timeout

Configuration property for the gateway client.

**Default Value (from template):** `${client.timeout:4000}`

This property configures the `request.timeout` setting in the `client.yml` configuration file.
