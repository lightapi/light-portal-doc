# request.healthCheckEnabled

Background health check configuration for idle connections.

**Default Value (from template):** `${client.healthCheckEnabled:true}`

This property configures the `request.healthCheckEnabled` setting in the `client.yml` configuration file.
