# oauth.sign.key.server_url

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.signKeyServerUrl:}`

This property configures the `oauth.sign.key.server_url` setting in the `client.yml` configuration file.
