# oauth.token.serviceId

Configuration property for the gateway client.

**Default Value (from template):** `${client.tokenServiceId:}`

This property configures the `oauth.token.serviceId` setting in the `client.yml` configuration file.
