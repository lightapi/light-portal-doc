# tls.tlsVersion

Specific TLS version to enforce (e.g., TLSv1.2, TLSv1.3).

**Default Value (from template):** `${client.tlsVersion:null}`

This property configures the `tls.tlsVersion` setting in the `client.yml` configuration file.
