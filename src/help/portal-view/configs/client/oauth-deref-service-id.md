# oauth.deref.serviceId

Configuration property for the gateway client.

**Default Value (from template):** `${client.derefServiceId:}`

This property configures the `oauth.deref.serviceId` setting in the `client.yml` configuration file.
