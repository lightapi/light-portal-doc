# request.poolWarmUpSize

Connection pool observability and warm-up configurations.

**Default Value (from template):** `${client.poolWarmUpSize:1}`

This property configures the `request.poolWarmUpSize` setting in the `client.yml` configuration file.
