# oauth.token.token_exchange.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenExClientId:}`

This property configures the `oauth.token.token_exchange.client_id` setting in the `client.yml` configuration file.
