# tls.clientKeyPath

Path to the certificate or key file.

**Default Value (from template):** `${client.clientKeyPath:}`

This property configures the `tls.clientKeyPath` setting in the `client.yml` configuration file.
