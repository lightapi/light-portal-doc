# oauth.token.tokenRenewBeforeExpired

Time in milliseconds for token retry delays and renewals.

**Default Value (from template):** `${client.tokenRenewBeforeExpired:60000}`

This property configures the `oauth.token.tokenRenewBeforeExpired` setting in the `client.yml` configuration file.
