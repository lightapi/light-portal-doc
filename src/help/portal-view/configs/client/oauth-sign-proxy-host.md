# oauth.sign.proxyHost

Forward proxy host or port.

**Default Value (from template):** `${client.signProxyHost:}`

This property configures the `oauth.sign.proxyHost` setting in the `client.yml` configuration file.
