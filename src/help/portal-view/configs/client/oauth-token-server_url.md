# oauth.token.server_url

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenServerUrl:}`

This property configures the `oauth.token.server_url` setting in the `client.yml` configuration file.
