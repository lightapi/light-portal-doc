# oauth.sign.timeout

Configuration property for the gateway client.

**Default Value (from template):** `${client.signTimeout:2000}`

This property configures the `oauth.sign.timeout` setting in the `client.yml` configuration file.
