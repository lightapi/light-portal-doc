# request.injectCallerId

Configuration property for the gateway client.

**Default Value (from template):** `${client.injectCallerId:false}`

This property configures the `request.injectCallerId` setting in the `client.yml` configuration file.
