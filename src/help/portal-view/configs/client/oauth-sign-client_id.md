# oauth.sign.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.signClientId:}`

This property configures the `oauth.sign.client_id` setting in the `client.yml` configuration file.
