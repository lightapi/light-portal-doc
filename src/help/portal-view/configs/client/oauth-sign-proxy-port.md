# oauth.sign.proxyPort

Forward proxy host or port.

**Default Value (from template):** `${client.signProxyPort:}`

This property configures the `oauth.sign.proxyPort` setting in the `client.yml` configuration file.
