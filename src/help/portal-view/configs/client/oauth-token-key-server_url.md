# oauth.token.key.server_url

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenKeyServerUrl:}`

This property configures the `oauth.token.key.server_url` setting in the `client.yml` configuration file.
