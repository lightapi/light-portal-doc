# oauth.token.client_credentials.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenCcClientSecret:}`

This property configures the `oauth.token.client_credentials.client_secret` setting in the `client.yml` configuration file.
