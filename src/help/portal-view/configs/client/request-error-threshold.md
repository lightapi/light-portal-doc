# request.errorThreshold

Number of errors before a connection is marked faulty.

**Default Value (from template):** `${client.errorThreshold:2}`

This property configures the `request.errorThreshold` setting in the `client.yml` configuration file.
