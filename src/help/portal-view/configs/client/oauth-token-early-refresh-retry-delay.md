# oauth.token.earlyRefreshRetryDelay

Time in milliseconds for token retry delays and renewals.

**Default Value (from template):** `${client.earlyRefreshRetryDelay:30000}`

This property configures the `oauth.token.earlyRefreshRetryDelay` setting in the `client.yml` configuration file.
