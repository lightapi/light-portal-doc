# oauth.token.token_exchange.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenExClientSecret:}`

This property configures the `oauth.token.token_exchange.client_secret` setting in the `client.yml` configuration file.
