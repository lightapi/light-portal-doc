# oauth.token.refresh_token.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenRtClientSecret:}`

This property configures the `oauth.token.refresh_token.client_secret` setting in the `client.yml` configuration file.
