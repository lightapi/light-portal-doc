# request.enableHttp2

Whether to use HTTP/2 multiplexing for the connection.

**Default Value (from template):** `${client.enableHttp2:true}`

This property configures the `request.enableHttp2` setting in the `client.yml` configuration file.
