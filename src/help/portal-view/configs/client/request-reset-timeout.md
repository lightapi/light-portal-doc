# request.resetTimeout

Timeout limits in milliseconds for establishing connections and reading responses.

**Default Value (from template):** `${client.resetTimeout:7000}`

This property configures the `request.resetTimeout` setting in the `client.yml` configuration file.
