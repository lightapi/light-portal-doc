# oauth.sign.serviceId

Configuration property for the gateway client.

**Default Value (from template):** `${client.signServiceId:}`

This property configures the `oauth.sign.serviceId` setting in the `client.yml` configuration file.
