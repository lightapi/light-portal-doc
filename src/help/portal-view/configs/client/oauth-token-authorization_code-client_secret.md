# oauth.token.authorization_code.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenAcClientSecret:}`

This property configures the `oauth.token.authorization_code.client_secret` setting in the `client.yml` configuration file.
