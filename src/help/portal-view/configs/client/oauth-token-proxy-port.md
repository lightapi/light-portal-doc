# oauth.token.proxyPort

Forward proxy host or port.

**Default Value (from template):** `${client.tokenProxyPort:}`

This property configures the `oauth.token.proxyPort` setting in the `client.yml` configuration file.
