# oauth.sign.key.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.signKeyClientId:}`

This property configures the `oauth.sign.key.client_id` setting in the `client.yml` configuration file.
