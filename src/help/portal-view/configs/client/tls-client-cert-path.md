# tls.clientCertPath

Path to the certificate or key file.

**Default Value (from template):** `${client.clientCertPath:}`

This property configures the `tls.clientCertPath` setting in the `client.yml` configuration file.
