# oauth.token.client_credentials.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenCcClientId:}`

This property configures the `oauth.token.client_credentials.client_id` setting in the `client.yml` configuration file.
