# oauth.sign.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.signClientSecret:}`

This property configures the `oauth.sign.client_secret` setting in the `client.yml` configuration file.
