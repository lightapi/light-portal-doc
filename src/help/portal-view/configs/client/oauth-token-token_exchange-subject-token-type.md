# oauth.token.token_exchange.subjectTokenType

Configuration property for the gateway client.

**Default Value (from template):** `${client.subjectTokenType:}`

This property configures the `oauth.token.token_exchange.subjectTokenType` setting in the `client.yml` configuration file.
