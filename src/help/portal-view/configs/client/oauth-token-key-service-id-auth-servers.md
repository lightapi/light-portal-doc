# oauth.token.key.serviceIdAuthServers

Mapping of service IDs to specific Auth server configurations when multipleAuthServers is true.

**Default Value (from template):** `${client.tokenKeyServiceIdAuthServers:{}}`

This property configures the `oauth.token.key.serviceIdAuthServers` setting in the `client.yml` configuration file.
