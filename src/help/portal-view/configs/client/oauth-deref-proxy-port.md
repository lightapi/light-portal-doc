# oauth.deref.proxyPort

Forward proxy host or port.

**Default Value (from template):** `${client.derefProxyPort:}`

This property configures the `oauth.deref.proxyPort` setting in the `client.yml` configuration file.
