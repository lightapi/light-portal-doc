# oauth.deref.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.derefClientSecret:}`

This property configures the `oauth.deref.client_secret` setting in the `client.yml` configuration file.
