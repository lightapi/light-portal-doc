# oauth.token.authorization_code.scope

OAuth scopes requested for the token.

**Default Value (from template):** `${client.tokenAcScope:[]}`

This property configures the `oauth.token.authorization_code.scope` setting in the `client.yml` configuration file.
