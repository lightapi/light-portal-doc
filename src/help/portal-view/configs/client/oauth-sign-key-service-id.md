# oauth.sign.key.serviceId

Configuration property for the gateway client.

**Default Value (from template):** `${client.signKeyServiceId:}`

This property configures the `oauth.sign.key.serviceId` setting in the `client.yml` configuration file.
