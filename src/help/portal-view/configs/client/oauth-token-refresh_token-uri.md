# oauth.token.refresh_token.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenRtUri:/oauth2/token}`

This property configures the `oauth.token.refresh_token.uri` setting in the `client.yml` configuration file.
