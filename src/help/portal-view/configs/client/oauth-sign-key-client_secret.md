# oauth.sign.key.client_secret

Client credentials used to authenticate.

**Default Value (from template):** `${client.signKeyClientSecret:}`

This property configures the `oauth.sign.key.client_secret` setting in the `client.yml` configuration file.
