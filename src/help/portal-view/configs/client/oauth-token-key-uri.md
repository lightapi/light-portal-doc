# oauth.token.key.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenKeyUri:/oauth2/key}`

This property configures the `oauth.token.key.uri` setting in the `client.yml` configuration file.
