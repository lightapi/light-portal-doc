# oauth.deref.proxyHost

Forward proxy host or port.

**Default Value (from template):** `${client.derefProxyHost:}`

This property configures the `oauth.deref.proxyHost` setting in the `client.yml` configuration file.
