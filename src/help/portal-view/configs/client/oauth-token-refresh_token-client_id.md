# oauth.token.refresh_token.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.tokenRtClientId:}`

This property configures the `oauth.token.refresh_token.client_id` setting in the `client.yml` configuration file.
