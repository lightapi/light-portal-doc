# request.connectionExpireTime

Connection pooling sizes, limits, and expiration times.

**Default Value (from template):** `${client.connectionExpireTime:1800000}`

This property configures the `request.connectionExpireTime` setting in the `client.yml` configuration file.
