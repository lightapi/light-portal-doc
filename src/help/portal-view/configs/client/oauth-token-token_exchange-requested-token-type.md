# oauth.token.token_exchange.requestedTokenType

Configuration property for the gateway client.

**Default Value (from template):** `${client.requestedTokenType:}`

This property configures the `oauth.token.token_exchange.requestedTokenType` setting in the `client.yml` configuration file.
