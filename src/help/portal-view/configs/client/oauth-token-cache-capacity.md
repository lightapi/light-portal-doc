# oauth.token.cache.capacity

Maximum number of items to keep in the token cache.

**Default Value (from template):** `${client.tokenCacheCapacity:200}`

This property configures the `oauth.token.cache.capacity` setting in the `client.yml` configuration file.
