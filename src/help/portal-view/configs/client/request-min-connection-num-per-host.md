# request.minConnectionNumPerHost

Configuration property for the gateway client.

**Default Value (from template):** `${client.minConnectionNumPerHost:250}`

This property configures the `request.minConnectionNumPerHost` setting in the `client.yml` configuration file.
