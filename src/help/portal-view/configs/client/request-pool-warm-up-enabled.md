# request.poolWarmUpEnabled

Connection pool observability and warm-up configurations.

**Default Value (from template):** `${client.poolWarmUpEnabled:false}`

This property configures the `request.poolWarmUpEnabled` setting in the `client.yml` configuration file.
