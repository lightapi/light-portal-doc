# oauth.token.token_exchange.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.tokenExUri:/oauth2/token}`

This property configures the `oauth.token.token_exchange.uri` setting in the `client.yml` configuration file.
