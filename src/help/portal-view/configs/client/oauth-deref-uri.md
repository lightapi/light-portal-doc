# oauth.deref.uri

The endpoint URL/URI for the specific OAuth grant or service.

**Default Value (from template):** `${client.derefUri:/oauth2/deref}`

This property configures the `oauth.deref.uri` setting in the `client.yml` configuration file.
