# oauth.deref.client_id

Client credentials used to authenticate.

**Default Value (from template):** `${client.derefClientId:}`

This property configures the `oauth.deref.client_id` setting in the `client.yml` configuration file.
