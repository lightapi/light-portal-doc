# oauth.multipleAuthServers

Whether the gateway interacts with multiple distinct OAuth servers.

**Default Value (from template):** `${client.multipleAuthServers:false}`

This property configures the `oauth.multipleAuthServers` setting in the `client.yml` configuration file.
